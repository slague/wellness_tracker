<%= form_tag admin_winners_path  do %>

    <%= label_tag :week_id %>
    <%= select_tag :week_id, options_from_collection_for_select(@mod_weeks, "id", "start_date")%><br><br>

    <%= submit_tag "Select a winner!", class: 'btn btn-custom'%>
<% end %>
