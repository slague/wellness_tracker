<hr>
  <div>
    <%= link_to "Past goals" %> &nbsp;
    <%= link_to "Turing Totals", goals_path %> &nbsp;&nbsp;
  <span>
    <%= image_tag('schedule-reminder.svg', size: '18x18') %>
    <%= link_to "Sign up for text reminders", edit_user_path(current_user) %>&nbsp;&nbsp;
  </span>
  </div>
<hr>
  <h2>Welcome, <%= current_user.name %></h2>
  <h3>This week is: <%=current_week.week_number.name%></h3><h4><%= current_week.format_start_date %>-<%= current_week.format_end_date %></h4>
<hr>

<div class="admin row">
  <!-- begin tabbed section -->
  <div class="card col-md-9" id="toggle">
    <div class="card_header">
      <ul class = "nav nav-tabs card-header-tabs">
        <li class="nav-item">
          <a class="nav-link active" data-toggle="tab" href="#">Set Weeks</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-toggle="tab" href="#">Weekly Winners</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-toggle="tab" href="#">Mod winners</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" data-toggle="tab" href="#">Cohorts</a>
        </li>
      </ul>

      <div class="tab-content">
        <div class="tab-pane active" id="overview" role="tabpanel">
          <%= render  %>
          <div class="text-left">
          <%# if @property.owner == current_user %>
            <%#= link_to "Edit this property", edit_property_path(@property), class: 'btn btn-custom' %><br><br>
            <%#= link_to "View property availability", property_property_availabilities_path(@property), class: 'btn btn-custom' %>
          <% #end %>
        </div>
      </div>


















<%= link_to "Set Weeks", new_admin_week_path%>&nbsp;&nbsp;Weekly winners&nbsp;&nbsp;Mod winners
   <hr>
<% @weeks.each do |week| %>
 <h6><%= week.format_start_date %>-<%= week.format_end_date %></h6>

<!-- NEEDS REFACTORING -->
    <%User.achievers(week.id).each do |user|%>
    <%= user.name %>
    <%end %>
 <%end%>


LINK TO SEE THIS MOD'S USERS W/ 100% goal achievement <br>
